server:
  port: 6009
  servlet:
    context-path: /

sping:
  applocation:
    name: report
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://192.168.1.224:3306/interface-test?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=UTC
    username: root
    password: 000000

mybatis:
  mapper-locations: classpath:mapping/*Mapper.xml
  type-aliases-package: com.interfacetest.entity

  #showSql
  logging:
    level:
      com:
        example:
          mapper: debug
